<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Administrar Citas - CRUD</title>
    <link rel="stylesheet" href="/style.css" />
    <style>
      .admin-table {
        width: 100%;
        border-collapse: collapse;
        margin-top: 20px;
      }
      .admin-table th,
      .admin-table td {
        border: 1px solid #ddd;
        padding: 12px;
        text-align: left;
      }
      .admin-table th {
        background-color: #007bff;
        color: white;
        font-weight: 600;
      }
      .admin-actions a,
      .admin-actions button {
        margin-left: 5px;
        cursor: pointer;
        padding: 8px 12px;
        border: none;
        border-radius: 5px;
        color: white;
        font-size: 0.9em;
        text-decoration: none;
      }
      .btn-edit {
        background-color: #28a745;
      }
      .btn-delete {
        background-color: #dc3545;
      }
      .btn-new {
        background-color: #ffc107;
        color: #333;
        padding: 10px 20px;
        text-decoration: none;
        border-radius: 5px;
        font-weight: bold;
        display: inline-block;
        margin-bottom: 20px;
        transition: background-color 0.2s;
      }
      .btn-new:hover {
        background-color: #e0b400;
      }

      @media (max-width: 600px) {
        .admin-table,
        .admin-table thead,
        .admin-table tbody,
        .admin-table th,
        .admin-table td,
        .admin-table tr {
          display: block;
        }
        .admin-table thead tr {
          position: absolute;
          top: -9999px;
          left: -9999px;
        }
        .admin-table tr {
          border: 1px solid #ccc;
          margin-bottom: 10px;
          border-radius: 8px;
        }
        .admin-table td {
          border: none;
          border-bottom: 1px solid #eee;
          position: relative;
          padding-left: 50%;
          text-align: right;
        }
        .admin-table td:before {
          position: absolute;
          top: 6px;
          left: 6px;
          width: 45%;
          padding-right: 10px;
          white-space: nowrap;
          text-align: left;
          font-weight: bold;
          color: #555;
        }
        .admin-table td:nth-of-type(1):before {
          content: "Cita:";
        }
        .admin-table td:nth-of-type(2):before {
          content: "Autor:";
        }
        .admin-table td:nth-of-type(3):before {
          content: "Acciones:";
        }
        .admin-actions {
          text-align: right;
          padding-right: 0 !important;
        }
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>Administración de Citas (CRUD)</h1>

      <a href="/admin/new" class="btn-new">➕ Añadir Nueva Cita</a>
      <a href="/" class="service-link">Volver a la Página Principal</a>

      <% if (quotes.length === 0) { %>
      <p>
        No hay citas en la base de datos. ¡Añade una para demostrar la
        funcionalidad!
      </p>
      <% } else { %>
      <table class="admin-table">
        <thead>
          <tr>
            <th>Texto</th>
            <th>Autor</th>
            <th style="width: 150px">Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% quotes.forEach(quote => { %>
          <tr>
            <td>
              <%= quote.text.substring(0, 80) %><% if (quote.text.length > 80) {
              %>...<% } %>
            </td>
            <td><%= quote.author %></td>
            <td class="admin-actions">
              <!-- Botón Editar (Update) -->
              <a href="/admin/edit/<%= quote._id %>" class="btn-edit">
                Editar
              </a>

              <!-- Formulario para Eliminar (Delete) -->
              <form
                action="/admin/delete/<%= quote._id %>"
                method="POST"
                style="display: inline"
                onsubmit="return confirm('¿Está seguro de eliminar esta cita? ESTA ACCIÓN ES PERMANENTE.');"
              >
                <button type="submit" class="btn-delete">Eliminar</button>
              </form>
            </td>
          </tr>
          <% }); %>
        </tbody>
      </table>
      <% } %>
    </div>
  </body>
</html>
